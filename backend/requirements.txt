fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
mutagen>=1.47
pillow>=10
python-multipart>=0.0.9
pymobiledevice3>=11
# construct-typing 0.8+ rejects the fork's AFC dataclasses (DataclassFieldError
# on import), which silently kills every AFC/DVT path including Preview shots.
# Verified 2026-09-24: 0.8.1 breaks, 0.7.0 works.
construct-typing<0.8
pytest>=8
httpx>=0.27
