# freetunes banner prompt (ChatGPT)

Copy-paste into ChatGPT with image generation on:

```
Widescreen 16:9 banner for an open-source app called "freetunes", 1600x900, dark modern Apple-style.

Background: dark navy gradient from #0B0E1A (top-left) to #1A1B3A (bottom-right), subtle purple/blue glow spots, faint dot grid, clean and minimal, no photo, no people.

Left side: rounded-square app icon, blue-to-purple gradient (#0A84FF to #BF5AF2), white music note symbol inside.

Title text (large, white, bold, sans-serif): "freetunes"
Subtitle text (smaller, light grey #AFB7D0): "FOSS web iTunes clone for iPhone"
Under subtitle: thin accent line, blue segment + short purple segment.

Below: 5 small dark pill badges with thin outline, white text:
"Music → VLC", "Books → Readest", "Audiobooks → BookPlayer", "Backup", "Screen • Photos • Diagnostics"
Each pill has a small blue dot on the left.

Right side: dark glass card containing a minimal iPhone silhouette (black, rounded, small notch), inside it 3 short horizontal bars (blue, purple, mint green). Below the phone, white text "Preview → Sync" and smaller grey text "AFC File Sharing".

Footer (small, muted grey): "FastAPI :8000 • Vite + React :5173 • USB via libimobiledevice" and second line "No iTunes • No Apple account • Nothing uploaded".

Style: flat vector, crisp, high contrast, professional GitHub README banner, generous padding, no watermark.
```

Tip: ChatGPT often mangles small text — if it does, regenerate with
`no text, background and icons only`, then overlay the exact text
in the current banner style (see `docs/banner.png`).
